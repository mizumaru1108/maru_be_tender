apiVersion: v1
kind: Service
metadata:
  name: {{ include "tmra-raise.fullname" . }}
  labels:
    {{- include "tmra-raise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tmra-raise.selectorLabels" . | nindent 4 }}
