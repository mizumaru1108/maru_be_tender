apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tmra-raise.fullname" . }}-test-connection"
  labels:
    {{- include "tmra-raise.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tmra-raise.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
